# Frontend environment example for Vite
# Copy to .env.local or set these variables in your shell before `npm run dev`
VITE_API_BASE_URL=http://localhost:8000
# Optional: override port used by Vite (default 5173)
# VITE_PORT=5173
